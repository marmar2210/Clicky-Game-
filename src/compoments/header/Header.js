import React from "react";
import "./Header.css";

const Header = pros => (
  <div className="header">
    <div className="title">{PaymentResponse.children}</div>
    <div className="scores">
      Score: {PaymentResponse.score} Highscore: {PaymentResponse.highscore}
    </div>
  </div>
);
export default Header;
